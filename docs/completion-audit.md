# Production validation record

This document records the acceptance coverage exercised in a private Cloudflare test
environment. Tenant identifiers, personal domains, account IDs, and live endpoints are
intentionally omitted from the public repository.

| Requirement | Evidence | Status |
|---|---|---|
| Fully online control plane | `zaodeploy-control-plane` Worker deployed with D1, R2, and Workflow bindings; local EZdeploy services were not running during Agent deployment | Passed |
| Online application center | `zaodeploy-app-center` Worker reads shared D1 and returns owner, stable URL, status, access mode, capabilities, and recent versions | Passed |
| Cloud state, no production SQLite | Production application/environment/deployment/event/resource rows queried from `zaodeploy-control` D1 | Passed |
| Durable bundle and task | Agent bundle stored in `zaodeploy-sources` R2 and executed by `zaodeploy-deployments` Workflow | Passed |
| Access protection | Anonymous control-plane, application-center, and organization-app requests returned Access redirects; service-token health requests succeeded | Passed |
| Multi-employee ownership | D1 stores scoped Agent token hashes; a second employee could list the catalog but received 403 for another owner's deployment, and 401 immediately after revocation | Passed |
| D1/R2/AI bindings | Example `/api/status` returned `database`, `storage`, and `ai` all `true` | Passed |
| Actual AI provider call | Example `/api/chat` returned HTTP 200 from `@cf/openai/gpt-oss-120b` through the scoped EZdeploy AI Proxy | Passed |
| First deployment below five minutes | Final destructive smoke first release completed in 31,840 ms | Passed |
| Resource reuse | Three production releases retained exactly three resource bindings total | Passed |
| Failed release isolation | Deliberately unhealthy smoke release persisted as failed while the previous healthy deployment remained active | Passed |
| Runtime logs and structured errors | MCP `get_zaodeploy_logs` captured a live Pages request URL, method, response status, and outcome through a short-lived Tail session; failed health verification stored `HEALTH_CHECK_FAILED` | Passed |
| Rollback | Third production release was followed by a Cloudflare Pages rollback to the second release and D1 active pointer update | Passed |
| Delete and cleanup | Temporary environment deletion removed Pages, D1, R2, AI credential, Access app, R2 artifact, and catalog visibility | Passed |
| Full tests and cloud gates | 39/39 automated tests, TypeScript build, Cloudflare preflight, and destructive real-cloud smoke passed | Passed |
| Zero-install Agent discovery | `agent.md`, `skill.md`, `agents.md`, `auth.md`, `llms.txt`, OpenAPI 3.1, capability discovery, standalone CJS client, WASM helper, and SHA-256 manifest are publicly reachable at the configured Agent domain | Passed |
| Real two-phase confirmation | The standalone client previewed the D1/R2/AI/organization plan twice without redeeming its code; deployment then redeemed it once and required the returned `planDigest` | Passed |
| Plan tamper protection | Changing the normalized manifest after preview caused `MANIFEST_INVALID: The project changed after the user confirmed the deployment plan` before upload | Passed |
| Exact enterprise application domain | Control plane uses the Pages API canonical subdomain, associates the hostname before creating its DNS-only CNAME, retries DNS validation, waits durably for certificate activation, and returns an application URL under the configured enterprise domain | Passed |
| Real zero-install full-stack release | Standalone downloaded client deployed Vite + Pages Functions + D1 + R2 + scoped AI + organization Access in 24 seconds after domain activation | Passed |
| Enterprise runtime verification | Anonymous health request returned Access redirect; service identity returned HTTP 200 with database/storage/AI all true; AI chat returned HTTP 200 from `@cf/openai/gpt-oss-120b` | Passed |
| Application-center registration | `/api/apps` lists owner, custom URL, ready sequence, organization access, all three resources, and failed/ready history | Passed |
| Real employee prompt issuance | In an authenticated employee browser session, the application center generated a copyable prompt containing the public Agent URL, two-hour code, plan-confirmation rule, `planDigest` rule, and enterprise-domain success requirement | Passed |
| UI-issued code interoperability | A code generated by the employee page was accepted by `/v1/connect/plan`, returned a confirmation-required organization plan, and remained unredeemed after preview | Passed |
| Current automated suite | 47/47 tests and TypeScript typecheck passed after the zero-install protocol and durable custom-domain changes | Passed |

Live demonstration endpoints are deployment-specific and are not committed. Operators should
record their own validation evidence outside the public repository.
